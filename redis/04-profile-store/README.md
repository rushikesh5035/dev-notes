# set -> Store single variable -> value complete delete, not update

# hset -> store object. -> value can update/change

# hgetall -> getting entire object

# hget ->

# hdel - delete

# hexists -> check field is available or not in memory
